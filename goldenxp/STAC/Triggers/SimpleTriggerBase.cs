namespace goldenxp
{
	using UnityEngine;
	
	[AddComponentMenu("")]
	public class SimpleTriggerBase : MonoBehaviour
	{
		/*
		// Triggers could use conditions like the code below
		// This could be in the base (this) or derived classes
		
		public SimpleConditionBase checkAgainst;
		public bool negateCheck;
		
		public bool CanTrigger() // <- can be called before calling Actions.Run()
		{
			if (checkAgainst == null) return true;
			return negateCheck ^ checkAgainst.Check();
		}
		*/
	}
}